<!DOCTYPE html>
<html lang="en"> 
    <%- include('../partials/head') %>
<body>   
    <%- include('../partials/header') %>
    <main class="main-login">
        <h3> INGRESAR</h3>
        <form  method="post" action="/user/login" class="form-login">
           
           
            <div class="grupo-ingreso">

                <input type="email" name="email" id="email-user" required value="<%= typeof old != 'undefined' ? old.email : '' %>">
                <label for="email-user" id="email-user">Email(*)</label>
           
                <% if(typeof(errors) != 'undefined' && errors.email){%>
                    <span style="color: red;">
                      <%= errors.email.msg %>
                    </span>
                 <%}%>
            </div>

            <div class="grupo-ingreso">
                <input type="password" name="password" id="password-user" required>
                <label for="password-user" id="password-user">Contraseña(*)</label>
            </div>

            

            <% if(typeof(errors) != 'undefined' && errors.password){%>
                <span style="color: red;">
                  <%= errors.password.msg %>
                </span>
             <%}%>
             <p >
                <input type="checkbox" name="recordarme"><label class="checkbox-inline">Recordarme</label>
             </p>

            <p>¿Nuevo cliente?<a href="/user/registro">Crear cuenta</a></p>

            <p>¿Olvidaste tu contraseña?<a href="#">Recuperar contraseña</a></p>

            <input type="submit" class="button-login" value="Ingresar">
        </form>
    </main>
    <%- include('../partials/footer') %>
</body>

</html>